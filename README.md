Data Pipeline and Analytics Engine
Overview

This project is a modular data pipeline and analytics engine that ingests CSV datasets, infers schema safely, validates data quality, persists results into SQLite, and runs analytics only when the data meaningfully supports them.

The system is designed to be defensive, explainable, and production-minded, avoiding misleading analytics when the data is unsuitable.
